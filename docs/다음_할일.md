# 다음에 할 일

*마지막 갱신: 2026-08-05*

통합본(모니터 + 분류자) Phase 1~7 완료 후 남은 작업. **사용자가 직접 해야 하는 것**과
**개발이 필요한 것**을 나눠 적는다.

---

## 🔴 지금 바로 — 사용자 액션

### 1. 마이그레이션 실행 (미완)
Supabase SQL Editor에서 아직 안 돌린 것:

```
backend/database/migrations/009_keyword_tuning.sql
```

무관 유입의 주범인 일반어 단독 키워드(`병무청`·`병역`)를 뉴스·지식인에서
사건성·조합형으로 교체한다. **이걸 돌려야 수집 단계부터 노이즈가 줄어든다.**

> 001~008은 적용 완료.

### 2. PR #12 병합
https://github.com/kjy-arch/safewatch-monitor/pull/12
부서 FK 수정 + 엑셀 18열 + 다운로드 버튼 + 비용 절감 3종.
병합 후 메인 저장소에서 `git pull` → `cd frontend && npm run build`.

### 3. 담당자 이름 변경
화면 우측 상단이 아직 **"검증담당"**(개발 중 넣은 값)이다. 실제 이름으로 바꿀 것.
이 값이 실행 이력·재분류 이력에 기록된다.

---

## 🟡 결정이 필요한 것

### 4. `frontend/dist` 커밋 여부 — 배포 시 결정
React 화면은 빌드 결과물(`frontend/dist`)이 있어야 뜨는데 `.gitignore` 대상이다.

- **Node.js 설치 가능한 PC** → 그대로 두기 (`setup.bat`이 자동 빌드)
- **설치 불가한 PC** → `dist`를 저장소에 커밋해야 함 (281KB)

설치 불가인데 그냥 두면 **구형 단일 화면으로 폴백**되어 보고서·검수·실행이력을 못 쓴다.

### 5. 분류자 저장소 은퇴
`safewatch-classifier`는 통합본에 흡수됐다. 롤백 경로로 남겨뒀지만
**같은 Supabase를 보고 있으므로 분류자로 새 작업은 하지 말 것.**
통합본이 충분히 검증되면 아카이브 처리.

### 6. D1 — 회색지대 소스 법무·정보보안 검토
디시인사이드·에펨코리아 수집 방법의 법적 정당성. **기관 검토 대기 중.**
현재는 기본 비활성이고 화면에서 명시 선택해야 실행되며 경고 팝업이 뜬다.
검토 전까지 이 상태를 유지할 것. 관련 미결 사항은 D2~D6.

---

## 🟢 개발 — 우선순위 순

### 7. 다음 수집 후 절감 효과 재측정
2026-08-05에 넣은 세 가지가 실제로 얼마나 줄이는지 확인:
- 병역 관련성 게이트 (`keyword_scorer.has_military_context`)
- 사전필터 임계치 3
- 본문 기준 중복 차단 (`crawlers/storage.py`)

로그에 `[storage] 본문 중복 N건 제외`가 찍히고, `false_reason`으로 게이트 처리
건수를 셀 수 있다. 009까지 적용한 뒤 측정해야 정확하다.

### 8. 출처 재배분 (측정 결과 반영)
2026-08-05 실측 — 같은 비용으로 산출을 2배 올릴 여지가 있다.

| 출처 | 건수 | 삭제대상 산출률 | 무관율 |
|---|---:|---:|---:|
| 지식인 | 180 | **11.7%** | **8.3%** |
| 유튜브 | 17 | 17.6% | 35.3% |
| 커뮤니티 | 203 | 7.9% | 37.9% |
| SNS(블로그) | 220 | **5.9%** | **45.5%** |
| 언론 | 56 | **1.8%** | 32.1% |

→ 지식인 키워드·수집량을 늘리고 블로그를 줄이는 방향. 언론은 수집 주기 완화 후보
(56건에서 삭제대상 1건, 대부분 사실 보도라 원래 삭제 제외 대상).

### 9. Q6 — 1년 보관 + 아카이빙 (미구현)
요구사항 20건 중 유일한 미구현 항목. 데이터가 쌓이기 전이라 급하지 않으나
운영 1년차에는 필요. 보관 정책 결정 → 아카이브 테이블 또는 엑셀 덤프 → 정기 실행.

### 10. 인증(로그인) — 배포 형태가 바뀔 때
현재는 담당자 PC 설치형이고 **인증이 없다.** 하루 1명씩 교대하는 지금 방식에서는
충분하지만, 아래 경우 로그인이 필수가 된다.
- 서버 1대에 여러 명이 접속하는 방식으로 전환 (실행 이력의 Windows 계정이
  전원 "서버 계정"으로 찍혀 무의미해진다)
- 감사에서 **부인방지**가 요구될 때 (현재 신원 기록은 위조 가능)

### 11. SNS 자격증명 확보
인스타그램·X·틱톡 크롤러는 구현돼 있으나 `is_active=false`.
키를 `.env`에 넣고 활성화하면 동작한다. 비용·심사 이슈 있음.

### 12. 잔여 정리
- `backend/check_dates.py`, `test_full.py`, `migrations.code-workspace` — 초기 실험물.
  `.gitignore`에 넣어뒀으나 삭제 여부는 미결.
  ⚠️ `test_full.py`는 실행 시 **실제 크롤링 + Gemini + 이메일 발송**이 일어난다.
- `compare_keyword_effect.py`, `eval_exclude_pilot.py`, `run_classify.py` — 운영 미사용.

---

## 참고 — 검증 명령

```
cd backend
for %f in (tests\test_*.py) do .venv\Scripts\python.exe %f    테스트 17종 (네트워크 불필요)

.venv\Scripts\python.exe validate_prefilter.py                사전필터 임계치 재검증
.venv\Scripts\python.exe eval_classifier.py --per-label 5     축 A 정확도 (Gemini 비용 발생)
.venv\Scripts\python.exe eval\eval_batch.py --golden eval\golden_set.csv   축 B 정확도
```

설치 확인: `http://localhost:8001/api/health` → `prefilter.enabled`가 `true`인지.
`false`면 키워드 사전이 없어 **모든 글이 Gemini로 간다**.
